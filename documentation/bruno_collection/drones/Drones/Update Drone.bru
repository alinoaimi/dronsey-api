meta {
  name: Update Drone
  type: http
  seq: 5
}

put {
  url: {{api_url}}/drones/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{admin1_token}}
}

body:json {
  {
    "status": "available"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
